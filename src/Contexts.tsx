import { createContext } from 'react';

// Define the types for each context

// Create contexts with default values
export const UserContext = createContext<any | undefined>(undefined);
export const AlertContext = createContext<any | undefined>(undefined);
export const CartContext = createContext<any | undefined>(undefined);
// src/Contexts.ts

